{
  "findings": [
    {
      "rule": "L005",
      "severity": "warn",
      "message": "SET NOT NULL scans users under ACCESS EXCLUSIVE to verify \"email\"; add CHECK (email IS NOT NULL) NOT VALID, validate it, then set NOT NULL",
      "detail": "ACCESS EXCLUSIVE on users, held for a full scan; blocks reads and writes",
      "file": "1_l005.sql",
      "step": "unprotected",
      "span": {
        "start": 26,
        "end": 75,
        "line": 2
      },
      "fix": "-- +mig step: users_email_not_null_check\n-- +mig satisfied: sql(SELECT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('users') AND conname = 'users_email_nn') OR coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('users') AND attname = 'email'), false))\n-- NOT VALID: the check exists without scanning anything\nALTER TABLE users ADD CONSTRAINT users_email_nn CHECK (email IS NOT NULL) NOT VALID;\n\n-- +mig step: validate_users_email_not_null\n-- +mig notx\n-- +mig satisfied: sql(SELECT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('users') AND conname = 'users_email_nn' AND convalidated) OR coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('users') AND attname = 'email'), false))\n-- the scan happens here, without blocking reads or writes\nALTER TABLE users VALIDATE CONSTRAINT users_email_nn;\n\n-- +mig step: set_users_email_not_null\n-- +mig satisfied: sql(SELECT coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('users') AND attname = 'email'), false))\n-- catalog only from Postgres 12: the validated check is the proof\nALTER TABLE users ALTER COLUMN email SET NOT NULL;\n\n-- +mig step: drop_users_email_check\n-- +mig satisfied: sql(SELECT NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('users') AND conname = 'users_email_nn'))\n-- the column carries NOT NULL itself now; the scaffolding leaves\nALTER TABLE users DROP CONSTRAINT users_email_nn;\n"
    },
    {
      "rule": "L005",
      "severity": "warn",
      "message": "SET NOT NULL scans payments under ACCESS EXCLUSIVE to verify \"ref\"; add CHECK (ref IS NOT NULL) NOT VALID, validate it, then set NOT NULL",
      "detail": "ACCESS EXCLUSIVE on payments, held for a full scan; blocks reads and writes",
      "file": "1_l005.sql",
      "step": "proof_on_another_table_does_not_count",
      "span": {
        "start": 610,
        "end": 660,
        "line": 20
      },
      "fix": "-- +mig step: payments_ref_not_null_check\n-- +mig satisfied: sql(SELECT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('payments') AND conname = 'payments_ref_nn') OR coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('payments') AND attname = 'ref'), false))\n-- NOT VALID: the check exists without scanning anything\nALTER TABLE payments ADD CONSTRAINT payments_ref_nn CHECK (ref IS NOT NULL) NOT VALID;\n\n-- +mig step: validate_payments_ref_not_null\n-- +mig notx\n-- +mig satisfied: sql(SELECT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('payments') AND conname = 'payments_ref_nn' AND convalidated) OR coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('payments') AND attname = 'ref'), false))\n-- the scan happens here, without blocking reads or writes\nALTER TABLE payments VALIDATE CONSTRAINT payments_ref_nn;\n\n-- +mig step: set_payments_ref_not_null\n-- +mig satisfied: sql(SELECT coalesce((SELECT attnotnull FROM pg_attribute WHERE attrelid = to_regclass('payments') AND attname = 'ref'), false))\n-- catalog only from Postgres 12: the validated check is the proof\nALTER TABLE payments ALTER COLUMN ref SET NOT NULL;\n\n-- +mig step: drop_payments_ref_check\n-- +mig satisfied: sql(SELECT NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conrelid = to_regclass('payments') AND conname = 'payments_ref_nn'))\n-- the column carries NOT NULL itself now; the scaffolding leaves\nALTER TABLE payments DROP CONSTRAINT payments_ref_nn;\n"
    },
    {
      "rule": "L005",
      "severity": "warn",
      "message": "SET NOT NULL scans users under ACCESS EXCLUSIVE to verify \"flag2\"; add CHECK (flag2 IS NOT NULL) NOT VALID, validate it, then set NOT NULL",
      "detail": "ACCESS EXCLUSIVE on users, held for a full scan; blocks reads and writes",
      "file": "1_l005.sql",
      "step": "combined",
      "span": {
        "start": 1201,
        "end": 1275,
        "line": 38
      }
    }
  ]
}
